Analyses student marks to give a short summary Implemented using python's inbuilt .open() method to open files and iterate through them to calculate the average, fetch highest and lowest scoring student with their marks.

python version - 3.14 (no additional modules/libraries are required)
